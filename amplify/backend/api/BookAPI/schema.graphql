type Session {
  date: String
  startPage: Int
  endPage: Int
}

type Book @model {
  id: ID!
  title: String!
  author: String!
  status: Int!
  description: String
  pageNumber: Int
  pages: Int
  tags: [String]
  queue_pos: Int
  startDate: String
  finishDate: String
  sessions: [Session]
}
